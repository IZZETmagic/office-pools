diff --git a/node_modules/@supabase/supabase-js/dist/index.cjs b/node_modules/@supabase/supabase-js/dist/index.cjs
index eaaca64..fba39b6 100644
--- a/node_modules/@supabase/supabase-js/dist/index.cjs
+++ b/node_modules/@supabase/supabase-js/dist/index.cjs
@@ -64,11 +64,13 @@ function __awaiter(thisArg, _arguments, P, generator) {
 let otelModulePromise = null;
 const OTEL_PKG = "@opentelemetry/api";
 function loadOtel() {
-	if (otelModulePromise === null) otelModulePromise = import(
-		/* webpackIgnore: true */
-		/* @vite-ignore */
-		OTEL_PKG
-).catch(() => null);
+	// Patched for React Native / Hermes compatibility: the original code used a
+	// dynamic import() with webpack/vite magic comments to optionally load
+	// @opentelemetry/api. Metro doesn't strip the magic comments and Hermes
+	// can't parse the resulting expression, breaking production builds. We
+	// don't ship @opentelemetry/api on mobile, so returning a resolved-null
+	// promise preserves the original "gracefully degrade" contract.
+	if (otelModulePromise === null) otelModulePromise = Promise.resolve(null);
 	return otelModulePromise;
 }
 /**
diff --git a/node_modules/@supabase/supabase-js/dist/index.mjs b/node_modules/@supabase/supabase-js/dist/index.mjs
index a34e281..72ac5a3 100644
--- a/node_modules/@supabase/supabase-js/dist/index.mjs
+++ b/node_modules/@supabase/supabase-js/dist/index.mjs
@@ -68,11 +68,13 @@ function __awaiter(thisArg, _arguments, P, generator) {
 let otelModulePromise = null;
 const OTEL_PKG = "@opentelemetry/api";
 function loadOtel() {
-	if (otelModulePromise === null) otelModulePromise = import(
-		/* webpackIgnore: true */
-		/* @vite-ignore */
-		OTEL_PKG
-).catch(() => null);
+	// Patched for React Native / Hermes compatibility: the original code used a
+	// dynamic import() with webpack/vite magic comments to optionally load
+	// @opentelemetry/api. Metro doesn't strip the magic comments and Hermes
+	// can't parse the resulting expression, breaking production builds. We
+	// don't ship @opentelemetry/api on mobile, so returning a resolved-null
+	// promise preserves the original "gracefully degrade" contract.
+	if (otelModulePromise === null) otelModulePromise = Promise.resolve(null);
 	return otelModulePromise;
 }
 /**
