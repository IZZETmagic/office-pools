import SwiftUI

// =============================================
// APP DESIGN TOKENS
//
// Border widths used across views.
// Spacing, radii, shadows, and typography live
// in SPDesign + Color.sp (the primary system).
// =============================================

enum AppDesign {

    // MARK: - Borders

    enum Border {
        static let thin: CGFloat = 0.5
        static let standard: CGFloat = 1
        static let accent: CGFloat = 1.5
    }
}
